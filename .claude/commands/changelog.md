Add a changelog entry for the work just completed in this conversation.

This repo has no `CHANGELOG.md` yet. Before creating one:

1. Check whether one has been added since this command was written (`ls` the
   repo root)
2. If it still doesn't exist, ask Bartosz whether to create one and where it
   should live before adding a new tracked file
3. If it exists, add a new entry following its existing format and section
   ordering (most recent first) — a short, factual line or two per change,
   not a full description; link back to `SAIGON_PROTOCOL_ARCHITECTURE.md`
   for detail rather than duplicating it
