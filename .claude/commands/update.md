Run the full Documentation Update Protocol from `.claude/commands/docs.md`
for the work just completed in this conversation:

1. Review what changed (systems designed/built, decisions made, open
   questions resolved, config changed)
2. Identify every affected doc using that file's File Routing Guide
3. Propose the specific changes for each file (section, new content, anything
   to remove) and wait for approval
4. Once approved, update the files one at a time — not as a single batch edit

Follow the Rules section in `.claude/commands/docs.md` exactly as written
there (tone matching, no unrelated audit, append-only running log, etc.).
